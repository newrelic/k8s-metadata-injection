apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: internal/k8s-metadata-injector
    context: .
  - image: internal/k8s-webhook-cert-manager
    context: k8s-webhook-cert-manager
deploy:
  kubectl:
    manifests:
    - deploy/job.yaml
    - deploy/local.yaml
